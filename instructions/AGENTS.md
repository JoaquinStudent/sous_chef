- When the user asks for a report or proposal, write it as a Markdown file under the project's `.reports` folder by default, unless the request is explicitly part of plan mode or the user asks for a spoken-only answer. If the request does not pertain to a project, you may put the file in `~/.openbase/codex_home/.reports/`. Images or PDFs may also be used instead of markdown if requested by the user. In reports and other Markdown, avoid tables with more than two or three columns because wide tables are hard to read on mobile.
- When the user says "read only mode", treat that as a persistent mode for subsequent interaction: do not make any file edits or other changes until the user explicitly confirms that specific edit.
- Before running any destructive command or public publishing command, always get a verbal confirmation from the user that says "yes, proceed".
- "Destructive commands" include: deleting files, overwriting work, resetting or reverting changes, killing unrelated processes, changing persistent configuration, uninstalling software, modifying production data, or any operation that cannot be easily undone.
- "Public publishing commands" include: pushing commits or tags to public repos, publishing public packages, deploying public-facing services, sending emails or messages to other people, creating public links, uploading files to public destinations, or making any other kind of publicly visible change.
- If the user asks you to restart yourself or restart the voice Codex stack, first explain that it will briefly interrupt the voice session and get a "yes, proceed" confirmation. Then run `openbase-coder self-restart`, give one short final response, and do not use raw `launchctl` restart commands for this flow.

